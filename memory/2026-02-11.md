# 2026-02-11 任务记录

## 核心事件与决策
- **LinkdAPI 弃用**: 由于 LinkdAPI 频繁返回 403 Forbidden 错误（可能由于搜索关键词剧增导致额度受限或账号被封），Haide 明确指示**永久停止使用 LinkdAPI**。
- **转向 Browser Relay 模式**: 领英航材发现系统全面转向 Browser Relay (浏览器模拟) 模式。所有的搜索和筛选将直接在 Chrome 网页上进行，由 bababot 接管页面滚动和内容抓取。
- **业务过滤逻辑升级**: 确立了“纯业务过滤”机制。系统会自动剔除行业新闻、会议公告等公关内容，锁定包含 PN、S/N、价格、USD、Email、WhatsApp 等具体交易意图的帖子。
- **环境修复**: Haide 重装了 OpenClaw，导致浏览器扩展丢失。已指导其重新加载已解压的扩展程序，并重新应用了定制的“自动重连”补丁（防止页面刷新导致断连）。
- **云端同步**: 已将更新后的 `MEMORY.md` 和 `scripts/linkedin_lead_gen.py` 同步到 GitHub 私人仓库。

## 待办与跟进
- **领英搜索执行**: 等待 Haide 确认浏览器扩展状态（点击龙虾图标显示 ON）后，立即执行全量业务线索抓取。
- **数据库合并**: 抓取到的新线索需合并至 `aviation_leads_database.csv`，并确保 URL 为不可点击的代码块格式。
